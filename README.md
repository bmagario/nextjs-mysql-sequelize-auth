## Create Docker
docker-compose up -d --build

## Run Docker

docker exec -it <DOCKER_NAME> bash

## Watch Docker's Logs
docker logs -f <DOCKER_HASH>

## Open Docker
docker exec -it <DOCKER_NAME> bash
